apiVersion: v1
kind: Service
metadata:
  name: dm-backend-api-fuseki-service
  namespace: application
  labels:
    app: dm-backend-api-fuseki
spec:
  selector:
    app: dm-backend-api-fuseki
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3030
  type: NodePort